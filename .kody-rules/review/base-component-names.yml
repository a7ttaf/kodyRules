title: Base component names
rule: >-
  Base components (a.k.a. presentational, dumb, or pure components) that apply
  app-specific styling and conventions should all begin with a specific prefix,
  such as `Base`, `App`, or `V`.
severity: low
examples: []
